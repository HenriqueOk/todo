ul
  - @tasks.each do |task|
    li
      = task.description
      - if task.completed_at.present?
        |  (Complete)
      - else
        = link_to 'Complete', complete_task_path(id: task.id), method: :post, class: "cpy-complete-task-#{task.id}"
      = link_to 'Edit', edit_task_path(id: task.id), class: "cpy-edit-task-#{task.id}"
      = link_to 'Delete', task_path(id: task.id), method: :delete, class: "cpy-delete-task-#{task.id}"
